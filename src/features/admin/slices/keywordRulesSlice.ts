import { createSlice } from '@reduxjs/toolkit';
const keywordRulesSlice = createSlice({ name: 'keywordRules', initialState: { items: [], isLoading: false }, reducers: {} });
export default keywordRulesSlice.reducer;
