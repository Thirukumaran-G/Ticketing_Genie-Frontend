import { createSlice } from '@reduxjs/toolkit';
const emailConfigSlice = createSlice({ name: 'emailConfig', initialState: { items: [], isLoading: false }, reducers: {} });
export default emailConfigSlice.reducer;
